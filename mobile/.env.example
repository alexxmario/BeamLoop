# BeamLoop production backend. Override this only when intentionally running
# the server locally; a physical device cannot reach your Mac via localhost.
EXPO_PUBLIC_API_URL=https://beamloop-production.up.railway.app
